name: gpuardian-web

services:
  gateway:
    image: gpuardian-web:local
    build:
      context: .
      dockerfile: Dockerfile.web
    restart: unless-stopped
    init: true
    user: "65532:65532"
    env_file:
      - /etc/gpuardian/web.env
    environment:
      GPUARDIAN_WEB_ADDR: 0.0.0.0:8443
      GPUARDIAN_WEB_TLS_CERT: /run/gpuardian-tls/web.crt
      GPUARDIAN_WEB_TLS_KEY: /run/gpuardian-tls/web.key
      GPUARDIAN_WEB_ALLOW_INSECURE: "0"
      GPUARDIAN_WEB_ALLOW_INSECURE_NODES: "0"
      GPUARDIAN_WEB_TRUST_PROXY: "0"
      GPUARDIAN_WEB_USERS: /var/lib/gpuardian-web/users.json
      GPUARDIAN_WEB_REGISTRY: /var/lib/gpuardian-web/servers.json
      GPUARDIAN_WEB_SESSION_KEY: /var/lib/gpuardian-web/session.key
      GPUARDIAN_WEB_USER_KEY: /var/lib/gpuardian-web/user-key.key
      GPUARDIAN_WEB_DB: /var/lib/gpuardian-web/history.db
      GPUARDIAN_WEB_UI_DIR: /usr/local/share/gpuardian/ui
    ports:
      - "8443:8443"
    volumes:
      - /var/lib/gpuardian-web:/var/lib/gpuardian-web
      - /etc/gpuardian/web.crt:/run/gpuardian-tls/web.crt:ro
      - /etc/gpuardian/web.key:/run/gpuardian-tls/web.key:ro
      # Use the gateway host's trust bundle so private node CAs installed on
      # that host are also trusted inside the container.
      - /etc/ssl/certs/ca-certificates.crt:/etc/ssl/certs/ca-certificates.crt:ro
    read_only: true
    tmpfs:
      - /tmp:rw,noexec,nosuid,nodev,size=16m
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    pids_limit: 256
    stop_grace_period: 10s
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: "3"
