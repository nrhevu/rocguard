name: rocguard-web-dev

services:
  gateway:
    image: rocguard-web:dev
    build:
      context: .
      dockerfile: Dockerfile.web
    restart: unless-stopped
    init: true
    user: "65532:65532"
    env_file:
      - ./.dev/web.env
    environment:
      ROCGUARD_WEB_ADDR: 0.0.0.0:8080
      ROCGUARD_WEB_TLS_CERT: ""
      ROCGUARD_WEB_TLS_KEY: ""
      ROCGUARD_WEB_ALLOW_INSECURE: "1"
      ROCGUARD_WEB_ALLOW_INSECURE_NODES: "1"
      ROCGUARD_WEB_SECURE_COOKIES: "0"
      ROCGUARD_WEB_TRUST_PROXY: "0"
      ROCGUARD_WEB_USERS: /var/lib/rocguard-web/users.json
      ROCGUARD_WEB_REGISTRY: /var/lib/rocguard-web/servers.json
      ROCGUARD_WEB_SESSION_KEY: /var/lib/rocguard-web/session.key
      ROCGUARD_WEB_USER_KEY: /var/lib/rocguard-web/user-key.key
      ROCGUARD_WEB_DB: /var/lib/rocguard-web/history.db
      ROCGUARD_WEB_UI_DIR: /usr/local/share/rocguard/ui
    extra_hosts:
      - host.docker.internal:host-gateway
    ports:
      - "127.0.0.1:18080:8080"
    volumes:
      - ./.dev/web:/var/lib/rocguard-web
    read_only: true
    tmpfs:
      - /tmp:rw,noexec,nosuid,nodev,size=16m
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    pids_limit: 256
    stop_grace_period: 10s
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: "3"
